name: Go Package Workflow
on:
  workflow_call:
jobs:
  ci-workflow:
    name: ci-workflow
    runs-on: ubuntu-latest
    steps:
      - run: .././.github/templates/go-test.yml
      - run: .././.github/templates/go-lint.yml
      - run: .././.github/templates/codeql.yml
